задание 46 язык C
#include <stdio.h>

int main() {
    int n = 2 + 1;  // Число, для которого будем находить факториал
    int factorial = 1; // Переменная для хранения результата
    int i = 1; // Счетчик цикла

    // Цикл с предусловием
    while (i <= n) {
        factorial *= i; // Умножаем текущий факториал на i
        i++;            // Увеличиваем счетчик
    }

    // Вывод результата
    printf("Факториал числа %d равен %d\n", n, factorial);

    return 0;
}
