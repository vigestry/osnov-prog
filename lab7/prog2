заданиие 29 язык Python
# Ввод размера массива
N = int(input("Введите размер массива N: "))

# Ввод элементов массива
A = []
print("Введите элементы массива:")
for i in range(N):
    A.append(int(input()))

# Инициализация переменной для минимального элемента
# Четные номера в Python имеют индексы 1, 3, 5 и т.д.
min_even_index = float('inf')  # Бесконечность для начальной проверки

# Поиск минимального элемента с четными номерами (индексы 1, 3, 5, ...)
for i in range(1, N, 2):  # Начиная с индекса 1 и шагая через один
    if A[i] < min_even_index:
        min_even_index = A[i]

# Проверка на наличие четных индексов
if min_even_index == float('inf'):
    print("В массиве нет элементов с четными номерами.")
else:
    print("Минимальный элемент с четным номером:", min_even_index)
