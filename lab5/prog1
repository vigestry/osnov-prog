задание 46 язык C
#include <stdio.h>

int main() {
    int n = 2 + 1; // Заданное значение числа (2 + 1)
    int factorial = 1; // Начальное значение факториала
    int i = 1; // Счетчик для цикла

    // Цикл с предусловием для вычисления факториала
    while (i <= n) {
        factorial *= i;
        i++;
    }

    // Вывод результата
    printf("Факториал числа %d! равен %d\n", n, factorial);

    return 0;
}
